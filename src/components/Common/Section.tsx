import styled from 'styled-components';

export const Section = styled.section`
    margin: 0;

    display: flex;
    flex-direction: column;
    gap: 0.5em;

    *  {
        margin: 0;
    }

    h2 {
        color: var(--color1);

        font-size: 1.5em;
        margin-bottom: 0.3em;
    }
`;
